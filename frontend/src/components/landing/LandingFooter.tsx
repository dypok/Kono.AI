/** Minimal marketing footer for the landing page. */
export function LandingFooter() {
  return (
    <footer className="mx-4 mb-4 mt-8 rounded-2xl border border-white/10 bg-slate-900/40 px-6 py-5 text-center backdrop-blur-xl">
      <p className="font-mono text-xs text-slate-500">
        Kono.ai &mdash; Deterministic invoice extraction, validation &amp; reconciliation. &copy; 2026.
      </p>
    </footer>
  );
}
